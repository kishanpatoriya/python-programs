## 📋 Program Index

1. **p01.py** - Display "Hello World." and "This is Python."
2. **p02.py** - Demonstrate built-in data types (`int`, `float`, `complex`, `bool`, `str`).
3. **p03.py** - Demonstrate built-in functions: `id()`, `type()`, and `range()`.
4. **p04.py** - Type conversion / casting functions (`int()`, `float()`, `str()`, etc.).
5. **p05.py** - Operators in Python (Arithmetic, Relational, Assignment, Logical, Bitwise, Ternary).
6. **p06.py** - I/O and formatting: `input()`, `print()`, `sep`, `end`, and replacement operator (`{}`).
7. **p07.py** - Conditional statements (`if`, `if-else`, `if-elif-else`).
8. **p08.py** - Iterative control structures (`while` loop and `for` loop).
9. **p09.py** - Loop control transfer statements (`break`, `continue`, `pass`).
10. **p10.py** - String accessing via positive/negative indexing and slice operator.
11. **p11.py** - File read and write operations.
12. **p12.py** - Copy content from one file to another file.
13. **p13.py** - Count the frequency of each character in a given text file.
14. **p14.py** - Read and print each line of a file in reverse order.
15. **p15.py** - Compute total number of characters, words, and lines in a file.
16. **p16.py** - Different ways of creating List objects.
17. **p17.py** - List methods (`list()`, `len()`, `count()`, `index()`, `append()`, `insert()`, `extend()`, `remove()`, `pop()`, `reverse()`, `sort()`, `copy()`, `clear()`).
18. **p18.py** - Different ways of creating Tuple objects.
19. **p19.py** - Pattern printing (Number triangle, Alphabet triangle, Inverted star pyramid).
20. **p20.py** - Tuple methods & operations (`len()`, `count()`, `index()`, `sorted()`, `min()`, `max()`, `reversed()`).
21. **p21.py** - Different ways of creating Set objects.
22. **p22.py** - Set methods (`add()`, `update()`, `copy()`, `pop()`, `remove()`, `discard()`, `clear()`, `union()`, `intersection()`, `difference()`).
23. **p23.py** - Different ways of creating Dictionary objects.
24. **p24.py** - Dictionary methods (`dict()`, `len()`, `clear()`, `get()`, `pop()`, `popitem()`, `keys()`, `values()`, `items()`, `copy()`, `update()`).
25. **p25.py** - Returning multiple values simultaneously using a return statement.
26. **p26.py** - Demonstration of Local scope vs Global scope variables.
27. **p27.py** - Anonymous/inline Lambda functions with examples.
28. **p28.py** - Calculate and display the exact year when the user turns 60 years old.
29. **p29.py** - Check whether a user input number is Odd or Even.
30. **p30.py** - Generate the Fibonacci sequence up to N terms.
31. **p31.py** - User-defined function to reverse any value/string.
32. **p32.py** - User-defined function to check if a number is an Armstrong number.
33. **p33.py** - User-defined function to check if a number/string is a Palindrome.
34. **p34.py** - Recursive function to compute the factorial of a given number.
35. **p35.py** - User-defined function to check for vowels (`True`/`False`) and compute list/string length.

---

## 🚀 How to Clone & Run

1. **Clone the repository:**
   ```bash
   git clone [https://github.com/kishanpatoriya/python-programs.git](https://github.com/kishanpatoriya/python-programs.git)
   cd python-programs